// 1. create a new file (or copy this one) in this folder named 'config.js'
// 2. fill in the API key and save in 'config.js' and save
// 3. config.js is already added to .gitignore so you don't have to worry
//    about your API key getting uploaded to github

const API_KEY = 'API_KEY_GOES_HERE';

module.exports.API_KEY = API_KEY;
